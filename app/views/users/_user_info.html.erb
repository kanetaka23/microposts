<aside class="col-md-4">
  <section class="user_info">
    <h1>
      <%= gravatar_for @user %>
      <%= @user.name %>
    </h1>
    <h2>
      <ul style="list-style:none;">
        <li>Location：<%= @user.location.present? ? @user.location : "(設定されていません)" %></li>
      <ul>
    </h2>
  </section>
  <p><%= link_to "user_page", @user %> | 
  <%= link_to "followings_page", followings_user_path(@user) %> | 
  <%= link_to "followers_page", followers_user_path(@user) %></p>
</aside>
  